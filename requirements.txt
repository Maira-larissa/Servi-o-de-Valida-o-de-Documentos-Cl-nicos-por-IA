# Dependências do projeto — Clinical Document Validator
# Compatível com Windows / macOS / Linux — Python 3.11+
# Instale com: pip install -r requirements.txt

# ── API ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9      # para File/Form upload no FastAPI
pydantic>=2.7.1
python-dotenv>=1.0.1

# ── Extração de texto (PDFs digitais) ─────────────────────────────────────
# PyMuPDF: instalado via pip, sem dependência de sistema
PyMuPDF>=1.24.3

# ── OCR (PDFs escaneados e imagens) ───────────────────────────────────────
# OCR 100% Python via ONNX Runtime, sem dependências de sistema
easyocr>=1.7.0
Pillow>=10.3.0

# ── Geração do dataset ─────────────────────────────────────────────────────
fpdf2>=2.7.9

# ── Avaliação / testes ─────────────────────────────────────────────────────
requests>=2.32.2
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0